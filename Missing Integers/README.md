Missing Integers in an Array (Java)

📌 Description

This Java program finds and prints all missing integers between 1 and the maximum element present in a given array.

If no numbers are missing, it prints:
No Missed Elements


🧠 How the Program Works

1. Reads an integer N (size of the array).

2. Reads N integers into an array.

3. Finds the maximum value in the array.

4. Uses a flag array to mark which numbers are present.

5. Prints all numbers between 1 and max that are missing.


🧾 Input Format

• First line: Integer N (number of elements)
• Next N lines (or space-separated): Integers of the array


📤 Output Format

• Prints each missing integer on a new line
                    OR
• Prints No Missed Elements if none are missing
